# 기본: 자동 감지
* text=auto

# Linux/Docker 실행 파일 → LF 강제
*.sh   text eol=lf
*.sql  text eol=lf
*.yml  text eol=lf
*.yaml text eol=lf

# Windows 텍스트 파일 → CRLF
*.txt  text eol=crlf
*.md   text eol=crlf

# 바이너리 (변환 안 함)
*.png  binary
*.jpg  binary
*.gif  binary
*.pdf  binary
